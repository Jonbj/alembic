[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
# Permette ai test di importare gli script standalone in scripts/ come moduli
# (es. compute_s4_ic, alpha_miner_dossier). La root del repo e' gia' in
# sys.path; scripts/ no, perche' non e' un package.
pythonpath = scripts
