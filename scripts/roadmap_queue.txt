# Coda della roadmap lavorabile durante il freeze di osservazione (#171).
#
# Letta dall'alto verso il basso da scripts/roadmap_agent_loop.sh: ogni run prende
# la PRIMA issue ancora aperta, con label `freeze-ok`, e senza PR aperta.
#
# Questo file e' meta' del lucchetto. L'altra meta' e' la label `freeze-ok` su
# GitHub. Una issue viene lavorata solo se compare in ENTRAMBI: la coda dice
# l'ordine, la label dice il permesso. L'agente in loop non puo' modificare ne'
# l'uno ne' l'altra — se lo facesse, deciderebbe da solo il proprio perimetro,
# che e' esattamente cio' che questo impianto esiste per impedire.
#
# Ordine: prima cio' che corrompe l'evidenza mentre la raccogliamo, poi cio' che
# serve alle decisioni di settembre, poi sicurezza e CI, poi il resto.
#
# Aggiornare l'ordine e' una decisione dell'operatore, non dell'agente.

# === PRIORITA' PERDITE S4 (aggiunte 2026-08-25) ===
# Bug che chiudono cause dirette di perdita giornaliera su S4.
# Tutte freeze-ok + ready-for-agent. In cima perche' ogni giorno di ritardo costa MTM.
327  S4: entrate sistematicamente tardie (percentile alto del range di giornata) — $52 MTM perso
328  S4: segnali WRONG_SIGN dal modello fallback + fan-out multi-ticker — rischio entrata nel verso sbagliato
335  S4: manca guard pre-trade earnings/price-action — aperto long su WMT in crollo -9,16% earnings
# 349 e' solo telemetria (alert falsa), non causale di perdite — in fondo al blocco
349  F-003: falsa alert di drawdown serale — costo_usd null, nessun impatto su ordini o P&L reale

191  il ratchet ha portato il gate S4 a 0,45: la finestra sta misurando la leva, non la strategia
208  il dossier non classifica la causa dei miss: la domanda di uscita n.1 non e' calcolabile al 28/09
190  i cron dell'osservazione perdono i propri fallimenti: un giro morto e uno sano hanno la stessa firma
184  exit_mechanism dedotto dall'eta' del segnale: etichetta MALE le righe che stiamo raccogliendo adesso
185  rebalance_frequency ignorata dal live: deroga approvata, corrompe l'evidenza S1 a ogni ciclo
161  alert posizioni non proteggibili: perimetro ristretto alla sola sorveglianza (vedi commento sull'issue)
134  ritiro di F8: premessa falsificata alla misura, leva gia' spenta (rimozione di codice inerte)
180  IC di S4 ricorrente: alimenta la decisione #179, che ora ha una soglia pre-registrata
176  backtest S4 non rappresentativo: il fallback sintetico silenzioso invalida ogni misura di backtest
151  segnali solo-fallback invisibili in execution_decisions: ripartire dalla PR #154 chiusa (prior art)
186  peso S4 azzerato con segnale preservato, sopra soglia e nei top-5
123  trades.signal_score senza signal_id corrispondente
177  dead config: from_yaml di S1/S3 non ha call site
181  overlap S1 intersezione S4: misura che serve a decidere #182
42   kill-switch resume non ripristina mode
41   frontend XSS: DOMPurify + CSP + token storage
43   rate limiting + CORS
152  test dipendente da reports/ gitignored: non passa in CI ne' in un clone fresco
50   frontend lint + test base
54   QX-01: migration schema 2-annotator + kappa harness + sampler
178  pulizia audit strategie: note stale, health_check no-op, milestone incoerente
39   S2-1: Source P&L Funnel completion
57   S2 VRP: fattibilita' strumento quotato, IBKR e dati
37   Vettore A: earnings chain, Phase 0 discovery

# Alpaca execution-layer hardening (audit 2026-08-07) — tutte freeze-ok.
# Spec + piani sul branch docs/spec-alpaca-hardening (mergiare a main prima: il loop lavora off main).
# #203 e' blocked_by #201: va lavorato DOPO che #201 e' mergiato+deployato (il loop lo salta finche' #201 resta aperto).
201  client_order_id sui 6 siti submit: dedup broker-side, safety-net doppio-submit (sblocca #203)
199  buying_power/multiplier mai gate-ati: pre-flight gate anti-reject-422 silenzioso
200  riconciliazione posizioni DB↔broker: beat EOD + alert + auto-close orphan (classe #121)
203  retry/backoff centralizzato retry_transient su reads+submit (blocked_by #201)

# Aggiunte del 2026-08-20 — le issue open con `freeze-ok` + `ready-for-agent`
# che non erano ancora in coda.
#
# Stesso criterio dell'header: prima cio' che corrompe l'evidenza mentre la
# raccogliamo, poi la strumentazione che serve alle decisioni del 28/09, poi il
# resto.
#
# Le due catene (alpha-miss, S4 exit trial) sono in ordine topologico: ogni
# issue compare DOPO i propri blocker. Il loop non legge `blocked_by`, quindi
# una issue bloccata in testa alla coda non fallisce — dichiara un no-op, e da
# quando il no-op non e' piu' un tentativo fallito quel no-op si ripete a ogni
# giro finche' il blocker non chiude. Un blocker che non chiudera' mai da solo
# e' quindi un muro permanente, non un'attesa: per questo NON compaiono qui le
# issue bloccate da una decisione umana (#297, #299, #300, #301 → #293/#244/#246)
# ne' quelle bloccate da una issue fuori rotazione (#287 → #281, 2 tentativi).
243  org_lookup attribuisce articoli a ticker che non compaiono nel testo: 97% di falsi positivi su MS e GS
245  cost model: 25 simboli di watchlist cadono nel fallback tier_d a 20 bps e falsano net_pnl a ogni trade nuovo
210  il realizzato di S1 e' un campione selezionato dalla regola d'uscita: il 28/09 lo leggerebbe come assenza di alpha
279  alpha-miss: copertura effettiva e attribution issuer-specific vs fan-out (radice della catena alpha-miss)
294  S4 exit trial: registrare gli intenti prima dei guard (radice della catena exit-trial)
282  alpha-miss: pannelli longitudinali e occurrence ledger (dopo #279)
284  alpha-miss: attribuzione P&L active/passive e qualita' decisionale (dopo #282)
283  alpha-miss: signal diagnostics e controlli negativi (dopo #279 e #282)
285  alpha-miss: contesto evento, regime e microstruttura (dopo #279)
286  alpha-miss: findings falsificabili e sintesi deterministica (dopo #282)
295  S4 exit trial: lifecycle, fill e sleeve virtuali (dopo #294)
296  S4 exit trial: riprodurre P0 come baseline shadow (dopo #295 e #245)
298  S4 exit trial: misurare replacement e opportunity cost (dopo #295 e #296)
214  le review verbose non arrivano su GitHub: il cancello di merge resta senza verbale

# Aggiunte del 2026-08-27 — issue open con `freeze-ok` + `ready-for-agent`
# non ancora in coda. Rilevate a coda vuota: il loop girava ma non pescava nulla.
#
# Prima la catena alpha-miss: #330 e #336 sono bug dell'infrastruttura del cron
# stesso (commit non pushati, worktree condiviso) — se la pipeline e' rotta le
# analisi a valle (#282-#286) lavorano su dati incompleti. Poi #333 (report
# omesse), poi i finding F-001/F-002 (qualita' dei dati di evidenza), infine
# #363 (PoC second broker, tier2).
330  Weekly: 2 alpha-miss reports stuck in local commits — automated analysis job does not sync before push (Aug 18-19 reports not on remote)
336  Alpha-miss cron runs in the shared working tree and inherits whatever branch is checked out: 2 of 5 runs never committed at all (corrects #330's 2-of-5 accounting to 4-of-5)
333  Alpha-miss report for Aug 20 omits 3 of 4 S4 entries and 3 of 4 exits, including the day's largest loss (HOOD -$60.32) — no report/dossier reconciliation
325  F-002 Weekly: 11 legacy positions missing stop_strategy for 14+ consecutive sessions — P&L attribution contamination at 58% of NAV on Aug 17
324  F-001 Weekly: 40% of watchlist has zero news coverage across Aug 17-20 — NO_NEWS is dominant miss cause (11/27 misses, $639+ lost alpha)
363  Second-broker PoC: congelare contratto comparabile e kill criteria

# Fuori coda di proposito, decisione dell'operatore (non dell'agente):
#   #281 alpha-miss funnel v2 — 2 tentativi falliti nello state.tsv, fuori
#        rotazione. Erano no-op onesti ("bloccata da #279/#280") addebitati
#        prima del fix del 2026-08-20: per rimetterla in rotazione va tolta la
#        riga da logs/roadmap_agent_state.tsv. #287 dipende da lei.
#   #314 SKIP_PYRAMIDING logga il peso target — gia' risolta nei fatti da #315
#        (PR #316 mergiata il 19/08: `score = quantity*price/nav` e
#        SKIP_PYRAMIDING nel counterfactual sono in main). Duplicato da chiudere.
#   #161, #199, #203 — gia' in coda sopra, ancora aperte con la loro PR mergiata.
#   #242, #244, #246, #293 — `ready-for-human` + `wayfinder:decision`.