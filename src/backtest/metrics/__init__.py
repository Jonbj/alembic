"""Backtest metrics engine (T-006).

Sub-modules:
  performance   — Sharpe, Sortino, Calmar, annualised return/vol, Omega
  risk          — max drawdown, VaR, ES, tail ratio, skewness, kurtosis
  signal_quality — IC, ICIR, p-value, Deflated Sharpe Ratio
  attribution   — per-strategy return and risk contribution
  report        — Markdown + HTML report generation
"""
