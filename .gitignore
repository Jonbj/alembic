# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
.venv/
venv/
env/
ENV/

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/

# Environment / secrets
.env
*.env
!.env.example

# IDEs
.idea/
.vscode/
*.swp
*.swo

# macOS
.DS_Store

# HuggingFace model cache (large, auto-downloaded)
.cache/

# Output reports
reports/

# Logs
*.log
logs/

# Database files
*.db
*.sqlite3

# Superpowers plugin internals
.superpowers/

# Jupyter
.ipynb_checkpoints/
*.ipynb
.worktrees/

# Backtest venv (host-side, not needed with Docker)
.venv-backtest/
# Firebase Android client configuration is deployment-specific and private.
/mobile/android/app/google-services.json
/mobile/android/app/src/debug/google-services.json
/mobile/android/app/src/release/google-services.json

# Android SDK / Gradle cache / JDK scaricati in locale (~5 GB). Ricostruibili,
# specifici della macchina, mai da versionare.
/.android-toolchain/

# Skill Claude Code installate da sorgenti esterne: sono una dipendenza,
# ricostruibile da skills-lock.json (che invece e' versionato).
/.claude/skills/
/.agents/

# Lock di sessione riscritto a ogni avvio: churn puro, non stato del progetto.
.claude/scheduled_tasks.lock

# Backup di dati DB prodotti dagli script one-shot: dati di ambiente, non di repo
backups/

# Stato runtime della notifica one-shot del kill criterion S4 (#180): e'
# informazione di quando l'ultimo PASS/FAIL e' stato annunciato, non un
# artefatto decisionale (quello vive in docs/evidence/s4_ic.json).
docs/evidence/s4_ic_notification.json

# Worktree temporanei lasciati dalle sessioni
.wt*/

# Log dei container preservati prima di un redeploy (workaround F-027).
# Servono alla sessione forense del giorno e poi diventano ridondanti: il report
# li riassume. Fuori da git per non aggiungere ~440K per ogni redeploy.
docs/evidence/logs_*/

# Chiavi SSH personali finite per errore nella root del repo (P8): vanno in
# ~/.ssh/, mai committate. Safety net anche dopo lo spostamento.
/stefano-nb1
/stefano-nb1.pub
/stefano-nb2
/stefano-nb2.pub
